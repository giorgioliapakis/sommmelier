[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "sommmelier"
version = "0.1.0"
description = "AI-driven Marketing Mix Modeling powered by Google Meridian"
readme = "README.md"
license = "MIT"
requires-python = ">=3.11,<3.13"
authors = [
    { name = "Giorgio Liapakis" }
]
keywords = ["mmm", "marketing-mix-model", "meridian", "marketing", "attribution", "ai", "claude"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
]

dependencies = [
    "google-meridian[schema]>=1.6.2,<1.7",
    "pandas>=2.0.0",
    "pyarrow>=14.0.0",
    "numpy>=2.0.0",
    "matplotlib>=3.8.0,<3.10",
    "typer>=0.9.0",
    "rich>=13.0.0",
    "pydantic>=2.0.0",
    "modal>=0.64.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "pytest-cov>=4.0.0",
    "ruff>=0.1.0",
    "mypy>=1.0.0",
    "pandas-stubs>=2.2.0",
]
gpu = [
    "google-meridian[and-cuda,schema]>=1.6.2,<1.7",
]

[project.scripts]
sommmelier = "mmm.cli.main:app"

[project.urls]
Homepage = "https://github.com/giorgioliapakis/sommmelier"
Repository = "https://github.com/giorgioliapakis/sommmelier"

[tool.hatch.build.targets.wheel]
packages = ["mmm"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP"]
ignore = ["E501"]  # Ruff's formatter intentionally leaves long strings and comments intact.

[tool.mypy]
python_version = "3.11"
strict = true
plugins = ["pydantic.mypy"]

[[tool.mypy.overrides]]
module = ["meridian.*", "tensorflow_probability.*"]
ignore_missing_imports = true

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-v"
